import forms from 'fzerocentral-web/utils/forms';
import { module, test } from 'qunit';

module('Unit | Utility | forms', function () {
  // Replace this with your real tests.
  test('it works', function (assert) {
    let result = forms();
    assert.ok(result);
  });
});
