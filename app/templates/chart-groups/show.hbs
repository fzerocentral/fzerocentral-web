<h1>{{@model.game.name}} - {{@model.name}}</h1>

{{#if @model.showChartsTogether}}
  <ChartRankingGroup
    @chartGroup={{@model}}
    @mainChartIdQueryArg={{mainChartId}}
    @recordRows={{recordRows}} />
{{else}}
  <ChartGroupContents @cg={{@model}} @root={{true}} />
{{/if}}
