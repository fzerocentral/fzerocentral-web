<h1>{{model.game.name}} - {{model.name}}</h1>

{{#if model.showChartsTogether}}
  {{chart-ranking-group chartGroup=model mainChart=mainChart}}
{{else}}
  {{chart-group-contents cg=model root=true}}
{{/if}}
