<table class="records-table">
  {{#each records as |record|}}
  <tr>
    <td>{{record.rank}}</td>
    <td>{{record.user.username}}</td>
    <td>{{record.value_display}}</td>
  </tr>
  {{/each}}
</table>
